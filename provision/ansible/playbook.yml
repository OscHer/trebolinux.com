---
- hosts: all
  become: yes
  tasks:
    - name: Say hello
      ping:

    - name: Ensure ansible_user
      ansible.builtin.user:
        name: "{{ item.name }}"
        shell: "{{ item.shell }}"
        groups: "{{ item.groups }}"
        state: "{{ item.state }}"
      with_items:
        - "{{ users }}"
